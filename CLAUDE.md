# CLAUDE.md — проектная память «Полировщик клипов»

## ✅ БОТ СТАЛ ПУБЛИЧНЫМ (сессия #6, 2026-07-26) — осталась живая проверка + релиз

**Цель:** приложение + бот ставит ЛЮБОЙ стример, скачав с github.com/GoLsik-web/clip-polisher.
Ни Python, ни config.json, ни токенов со сторонних сайтов.

**СДЕЛАНО И ПРОВЕРЕНО (тесты + офскрин-скрины, сеть подменена):**
1. **Вход через Twitch одной кнопкой** — `core/twitch_auth.py`, OAuth **Device Code Flow**
   (публичный клиент, без секрета). **Client ID = `hcvws331csxduqidpoccvuqbqxu8eh`** (пользователь
   зарегистрировал приложение «Clip Polisher», тип PUBLIC; ID не секретный — вшит в код).
   Проверено вживую: `/oauth2/device` отдаёт код. Scopes: `chat:read`,`chat:edit`. Токены —
   `%LOCALAPPDATA%\ClipPolisher\twitch_auth.json` (атомарно, chmod 600), `ensure_token()` сам
   рефрешит по refresh_token, `logout()` отзывает токен у Twitch. Helix (онлайн/старт эфира)
   ходит ПОЛЬЗОВАТЕЛЬСКИМ токеном — секрет не нужен.
2. **Движок бота переехал в `core/chatbot.py`** (был `bot/marks_collector.py`+`twitch_marks_bot.py`):
   `MarkCollector` (роли/команды/кулдаун/атомарная запись), `parse_irc`, `TwitchChat` (SSL-IRC,
   stop() будит recv, PING при тишине, `AuthFailed` = не переподключаться), `BotService` (фоновые
   потоки чат+онлайн, колбэк событий `status/mark/online/error/log`, экспоненциальный бэкофф
   переподключения 5→60с). Метки пишутся в `%LOCALAPPDATA%\ClipPolisher\marks\` (не в папку
   программы). `bot/*.py` остались ТОНКИМИ обёртками для консоли — старые тесты (22 ✔) проходят.
3. **Вкладка «Бот» в приложении** — `ui/bot_panel.py`, третья вкладка справа в режиме 2
   (`Моменты | Раскладка | Бот`). Карточки: АККАУНТ TWITCH (кнопка входа → крупный код +
   «Открыть страницу»/«Скопировать»/«Отмена» + обратный отсчёт; после входа «Вошёл как X» +
   «Выйти»), БОТ В ЧАТЕ (канал, кто может метить, пауза зрителя, ответы в чат, Подключить/
   Остановить, точка-статус + онлайн), МЕТКИ ЭФИРА (счётчик, живой журнал меток, «Взять эти метки
   в нарезку» → `set_marks_file`, «Папка с метками»). Потоки: `LoginThread`/`BotThread`/
   `_AccountThread`, гасятся в `MainWindow.closeEvent` через `bot_panel.shutdown()`.
   В левой колонке кнопка «Нет меток? Включить бота в чате» ведёт на эту вкладку.
4. **Тесты:** `dev/test_bot.py` (30 ✔ — device flow на подменённых ответах, понятные ошибки,
   хранение/рефреш токена, BotService целиком с чатом-заглушкой: события→UI, файл→моменты,
   остановка без подвисания, пути). `dev/shot_bot.py` — 6 скринов (вход/код/работа × тёмн/светл),
   `dev/shot_marks.py` доснимает вкладку в целом окне. Правки по скринам: тофу-квадрат вместо
   «✓» убран, у выключенных кнопок свой серый вид, строка паузы ужата.
5. **Сборка:** в `clip-polisher.spec` добавлены hiddenimports `core.chatbot`, `core.twitch_auth`
   (импорт ленивый, внутри функций). `bot/README.md` переписан под кнопку входа.

**АВТОПИЛОТ + ТРЕЙ + РАЗДЕЛЕНИЕ ЭФИРОВ (та же сессия, по вопросу пользователя):**
6. **🐞 БАГ (был, ПОФИКШЕН): второй эфир за день ЗАТИРАЛ метки первого.** Имя файла было только
   по дате (`<канал>_<дата>.clipmarks`) → новый `MarkCollector` писал в тот же путь пустым списком.
   Проверено тестом до фикса (3 метки → 1). Теперь `session_output()` = `<канал>_<дата>_<ЧЧ-ММ>_<id
   эфира>.clipmarks`, «сессия» на каждый эфир: свой файл, свой ноль времени (= `started_at` из Helix),
   в файле `broadcast_id`/`title`/`game`/`duration` (в `core/marks.MarksFile` добавлены `title`/`game`,
   старые файлы читаются). Перезапуск программы посреди стрима → `resume_existing()` дописывает тот же
   файл, а не затирает. Метки вне эфира — в файл `..._вне-эфира.clipmarks`.
7. **Автопилот** — `StreamWatcher` (опрос Helix раз в минуту, сглаживание офлайна `offline_grace=180с`,
   чтобы моргание сети не выключало бота) + `BotService(autopilot=True)`: эфир начался → бот сам зашёл
   в чат и завёл файл, кончился → вышел и закрыл файл, ждёт следующего. В UI — тумблер (по умолчанию
   ВКЛ) + автостарт бота после входа/запуска программы (`_auto_started`).
8. **Трей и автозапуск Windows** — `core/autostart.py` (HKCU\...\Run, значение ClipPolisher, команда с
   ключом `--tray`, без прав админа; `refresh_if_enabled()` чинит путь после обновления).
   `MainWindow._build_tray` (значок+меню Открыть/Выход, ⚠️ НЕ создавать на offscreen — процесс падал
   на выходе), `hide_to_tray`, закрытие окна при работающем боте сворачивает в трей
   (`tray_on_close`, QSettings("ClipPolisher","Bot")). `app.py --tray` — старт свёрнутым.
9. **Карточка эфира** в режиме нарезки: `_describe_stream()` → «Эфир 26 июля, 21:04 · «Название» ·
   2 ч 15 мин · 12 меток · онлайн 137» вместо имени файла; склонения `_plural_marks`. В именах клипов
   теперь дата эфира (`_stem()` = ник_ГГГГ-ММ-ДД).
10. **Гонка (пофикшена):** ответ фоновой проверки входа приходил ПОСЛЕ закрытия окна и поднимал бота
   на выходе → `_closing`-флаг в `BotPanel` (+ `_on_account`/`_start_bot`/`_refresh_account` его чтут),
   `validate()` с таймаутом 8с, `shutdown()` ждёт 6с и в крайнем случае снимает поток.
11. **Тесты:** `dev/test_bot.py` вырос до 51 ✔ (два эфира за день не смешиваются, перезапуск посреди
   стрима, автопилот: без эфира в чат не заходит → зашёл → вышел). `dev/test_autostart.py` — реальный
   реестр, состояние пользователя восстанавливается. `dev/shot_marks.py` ждёт QThread'ы перед выходом.

**✅ ЖИВАЯ ПРОВЕРКА ПОЛЬЗОВАТЕЛЕМ ПРОШЛА (2026-07-26 22:15):** вход через Twitch выполнен
(`%LOCALAPPDATA%\ClipPolisher\twitch_auth.json`, scopes chat:read/chat:edit), бот подключился,
метка `!clip` записалась (`marks/golsik___2026-07-26.clipmarks`, t=51.1с, роль streamer).
⚠️ Этот файл — со СТАРЫМ именем (до фикса), новые пойдут с временем и id эфира.

12. **Проверка без стрима (для друга-тестировщика):** кнопка **«Зайти в чат сейчас (проверка без
   эфира)»** — `BotService.connect_now()` (ставит `_live`, автопилот не выключается). Показывается,
   когда статус = «Автопилот: жду начала эфира…». Без неё автопилот молча ждёт трансляции и кажется
   сломанным. Инструкция для тестировщика — **`docs/ПРОВЕРКА-БОТА.md`** (простым языком, 10 минут).
   Тесты: `dev/test_bot.py` → 58 ✔ (блок [9] — весь сценарий проверки без эфира).

**🎨 ВИЗУАЛЬНАЯ ПОЛИРОВКА (сессия #6, по скринам — не «на глаз»):**
- **🐞 Светлая тема: тумблеры/чипы были почти чёрными** — `ToggleSwitch.paintEvent` и `Chip._restyle`
  держали константы тёмной темы. Теперь берут цвета из палитры через `theme.set_current()/current()`
  (новое в `ui/theme.py`), у выключенного тумблера дорожка = `line`, шарик = белый (светлая) /
  `muted` (тёмная) — иначе читался как «залипший». Перекраска: `MainWindow._apply_theme`,
  `MarksModePanel.apply_theme`, `BotPanel.apply_theme` обходят `findChildren(Chip/ToggleSwitch)`.
  Проверено ПИКСЕЛЯМИ (`grab().toImage().pixelColor`), а не глазами.
- **🐞 Пустой список моментов светился белым прямоугольником** на тёмной теме (QScrollArea viewport
  красился системным фоном). Фикс: прозрачные `QScrollArea`+viewport+host. Плюс пустое состояние —
  подсказка «Моментов пока нет… загрузи метки или включи бота» (`moments_empty`).
- **Линейка дорожки**: крайние подписи (`0:00`, `2:00:00`) обрезались — рамка текста вылезала за
  виджет. Теперь крайние прижаты внутрь (`_draw_ruler`).
- **Вкладка «Бот»**: поля переведены на `QFormLayout` (подписи и вводы по одной линии), строки
  тумблеров — через общий `_switch_row` (одна высота, без переносов), тексты укорочены.
- **Заголовок окна** менялся: всегда был «Этап 1» → теперь по режиму (`_on_mode_selected`).
- **Dev-скрипты**: `dev/screenshot.py`, `dev/test_multi.py`, `dev/shot_marks.py` гасят потоки панели
  бота перед выходом (иначе Qt рушил процесс: проверки печатали «пройдены», а код возврата 127).
  В `shot_marks` добавлен `_wait_threads()`.

**📦 СБОРКА v1.0.6 (2026-07-26, локально, НЕ опубликована):**
`installer\Output\ClipPolisher-Setup-1.0.6.exe` (216,5 МБ), `dist\ClipPolisher\` (810 МБ).
Пересобран 23:56 уже С визуальными правками.
⚠️ ГРАБЛЯ: смоук-тест exe оставлял процесс висеть (kill из bash убивал обёртку, не exe) — он держал
`dist\` и PyInstaller падал с `PermissionError`, а Inno молча собирал установщик из СТАРОЙ сборки.
После смоук-теста проверять `Get-Process ClipPolisher` и убивать через `Stop-Process`.
Версия поднята в `core/version.py` и `installer/clip-polisher.iss`. Проверено: модули
`core.chatbot/twitch_auth/autostart`, `ui.bot_panel` попали в сборку (Analysis-00.toc), exe стартует
офскрин 25с без падений. Релиз на GitHub НЕ выпускался (решение пользователя: сначала обкатать на
друге). Логи сборки: `out/build_1.0.6.log`, `out/iss_1.0.6.log`.
⚠️ Установку поверх ассистент не гонял (не трогал систему пользователя) — цепочка «install→run→
uninstall» проверялась на прошлых версиях, AppId и `CloseApplications=yes` не менялись.

**⬜ ОСТАЛОСЬ (следующая сессия):**
- **ЖИВАЯ ПРОВЕРКА АВТОПИЛОТА** (нужен реальный эфир — у ассистента его нет): включить
  автопилот, выйти в эфир → бот должен сам зайти в чат и завести файл с id эфира; закончить
  эфир → бот выходит, файл закрыт. Плюс трей: закрыть окно при работающем боте (должно
  свернуться), автозапуск с Windows (перезагрузка → значок у часов).
- Пересобрать exe+установщик и выпустить **v1.0.6** по релиз-чек-листу (версия в `core/version.py`
  И `installer/clip-polisher.iss`, `gh release create`, удалить прошлый релиз).
- Путь «по ссылке на запись стрима» (`link_edit`/`link_btn` — плейсхолдер): `yt-dlp
  --download-sections` только вокруг меток.
- Транскрипция всего файла на длинном стриме (медленно) — резать по окнам сегментов.

**ВАЖНО — предпочтение пользователя:** в git-коммитах/релизах НЕ указывать AI как соавтора
(никаких `Co-Authored-By: Claude`, никаких упоминаний ИИ). Коммиты — чистые, от лица пользователя.

**ЛИЧНАЯ настройка (уже работает, для теста/себя):** `bot/config.json` — канал GoLsik__, бот
ClipMarkBot, токен вставлен (в git не идёт). Бот проверен вживую (см. трек C ниже). `bot/run_bot.bat`
— запуск двойным кликом. Для публичной версии это всё заменит кнопка входа + встроенный бот.

---

## 📌 ЭТАП 2 — ЗАФИКСИРОВАННЫЙ ДИЗАЙН (сессия #4, 2026-07-19)

**Суть:** режим «Метки через бота» — из ОДНОГО стрима вырезать несколько разнесённых
моментов и склеить в ОДИН вертикальный клип. Моменты помечаются меткой в чате (свой бот).

**РЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ (locked, не переспрашивать):**
- **Метка = точка** («где-то тут момент»), не отрезок. Типы автора: `streamer / moderator / vip / viewer`.
- **Схлопывание** близких меток: разрыв ≤ **15 с** → один момент.
- **Окно** вокруг центра метки: **−30 / +10 с**, дальше ручная подгонка на таймлайне.
- **Отбор по режиму аудитории** (`AudienceMode`): доверенные (streamer/mod/vip) → момент ВСЕГДА;
  зрительские → только если разных зрителей ≥ порог. Пороги: камерный=1, средний=3, большой=10.
  По умолчанию **AUTO** (по онлайну из файла меток: <50 камерный, <500 средний, иначе большой),
  но выбор режима ВИДИМЫЙ — юзер явно видит, что стоит Авто, и может переключить.
  ⚠️ Названия мягкие: НЕ «мелкий стример», а про онлайн («камерный/средний/большой онлайн»).
- **Два способа загрузки:** (а) видео-файл + импорт файла меток; (б) ссылка на трансляцию (метки
  авто). По ссылке качать не весь VOD, а только окна вокруг меток (yt-dlp `--download-sections`).
- **Матчинг видео↔метки:** по длительности + попаданию времён меток в длину; расхождение → мягкое
  ПРЕДУПРЕЖДЕНИЕ (не запрет). Голый mp4 не хранит VOD-ID.
- **Неполная запись:** явный режим; юзер указывает обрезку в НАЧАЛЕ, КОНЦЕ и ДЫРЫ В СЕРЕДИНЕ (запись
  склеена/прерывалась). Нужен слой маппинга «время стрима → позиция в файле» (пока НЕ написан).
- **Ник/платформа** стримера подставляются из файла меток, с ручной правкой.
- **Панели-выключатели:** любую панель можно выключить (вебка/геймплей/субтитры/ник/лого) — «без
  вебки» = частный случай. Область настройки — переключатель **«весь стрим / только этот момент»**.
- **Продвинутый звук:** шумодав + чёткость голоса (лёгкий EQ) + громкость (loudnorm есть) + гейт.
  Подавление фоновой МУЗЫКИ — на будущее, пока НЕ делаем.
- **UI-идеи (одобрены):** тепловая дорожка меток (с выключателем); список «Моменты» с сортировкой по
  «жару» + кнопка «топ-N»; фильтр-ПОДСВЕТКА по типу автора (нажал «Стример» — остальные тускнеют,
  мультивыбор типов); умная подгонка границ по речи/звуку + ручная; заметка в метке (`!clip текст`)
  → имя момента/подсказка хук-заголовка.

**ТРИ ТРЕКА (делать ПАРАЛЛЕЛЬНО, где можно — просьба пользователя):**
- **A. Ядро склейки** — ✅ СДЕЛАНО и проверено в сессии #4:
  • `core/marks.py` — формат файла меток (`MarksFile`/`Mark`/`AuthorType`/`AudienceMode`) + логика
    `select_moments()` (схлопывание 15с→порог режима→окно→`Moment`), `resolve_mode`,
    `moments_to_segments`, `by_heat`. Всё во времени СТРИМА. Проверено `dev/test_marks.py`.
    ⚠️ ВАЖНО (правка сессии #4): перекрытие окон соседних моментов НЕ сливается в один, а
    РАЗВОДИТСЯ по середине между центрами → две разнесённые метки всегда дают ДВА клипа. Но
    метки ближе 15с — это по-прежнему ОДИН момент (правило схлопывания). Демо-файл
    `tests/sample_marks/egoric_demo.clipmarks` — метки в двух группах >15с врозь → 2 момента.
  • `core/timemap.py` — слой «неполная запись»: `TimeMap`/`Piece`, перевод время-стрима↔позиция-в-файле,
    `map_range`/`map_segments` (режут отрезок по дырам). Проверено `dev/test_timemap.py`.
  • `core/audio.py` — продвинутый звук: `denoise`/`clarity`/`gate` в `build_audio_filter` (+проброс
    через `render.RenderExtras` и `pipeline.PipelineConfig`). Проверено (тот же тест).
  • `pipeline.PipelineConfig.segments: Optional[list[Segment]]` + `run()` берёт список, если задан.
- **B. UI приложения** — ✅ СДЕЛАНО в сессии #4: `ui/marks_mode.py` (`MarksModePanel`):
  • `MarksTimeline` — тепловая дорожка плотности + пины по цвету автора (стример выше/ярче) +
    моменты-отрезки с перетаскиваемыми границами + штриховка дыр записи + линейка времени.
  • Левая колонка: Источник (видео+файл меток+«запись неполная»/сдвиг), Режим отбора (ВИДИМЫЙ
    Авто/Камерный/Средний/Большой + окно до/после + фильтр-подсветка по автору), Звук (loudnorm/
    шумодав/чёткость/гейт + мат), Брендинг+Экспорт (автоник/платформа + кнопка склейки).
  • Правая часть — сегментный переключатель [Моменты | Раскладка] (`QStackedWidget`): «Моменты» =
    таймлайн+список; «Раскладка» = ТОТ ЖЕ редактор Этапа 1 `EditorPanel(show_trim=False)` — ручное
    перетаскивание/масштаб/поворот зон + пресеты A/B/C/D + глаза-видимость + safe-зоны (правка
    сессии #4: раньше в режиме 2 были только пресеты — теперь полноценная ручная раскладка).
  • **Экспорт — 2 режима** (`export_chips`, дефолт РАЗДЕЛЬНО): «Отдельными клипами» = каждый момент
    в свой файл `<streamer>_NN_<заметка>_vertical.mp4` через `BatchRenderThread` (очередь);
    «Склеить в один» = все сегменты в один `<streamer>_montage.mp4` через `RenderThread`.
    `build_pipeline_configs()` возвращает список; `main_window._render_marks` выбирает поток и
    показывает итог (`_marks_batch_done`/`_render_done`). Раскладка/звук общие (из редактора).
  • **Цвета тегов авторов = как значки Twitch** (`AUTHOR_COLORS`): стример #e91916 (красный),
    модер #00ad03 (зелёный), вип #e005b9 (розовый), зритель #9146ff (Twitch purple).
  • Путь «по ссылке на трансляцию» — видимый, но ЗАБЛОКИРОВАННЫЙ плейсхолдер (`link_edit`/`link_btn`):
    автозагрузка меток + скачивание окон VOD появится ВМЕСТЕ с ботом (трек C).
  • Панель «Моменты»: карточки (чекбокс-включить, №, заметка-имя, диапазон, авторские точки,
    «жар» с нарисованной иконкой-пламенем — НЕ эмодзи, иначе тофу), сортировка по времени/жару,
    «Взять топ-5». Встроено в `main_window` через `QStackedWidget` (изоляция от Этапа 1, без
    регрессий): `_on_mode_selected` листает страницы, `_render_marks` собирает pcfg и рендерит через
    `W.RenderThread`. Проверено офскрин-скринами (тёмн/светл × 1280/1040) `dev/shot_marks.py` + smoke
    сборки окна и `build_pipeline_config` (сегменты/звук/ник корректны).
- **C. Бот Twitch** — ✅ СДЕЛАН (сессия #5), папка `bot/` (только stdlib, без внешних зависимостей):
  • `bot/marks_collector.py` — ЯДРО (тестируемо, без сети): `MarkCollector` (разбор команд
    !clip/!клип/!метка + заметка, роль по IRC-тегам `role_from_tags`, антиспам-кулдаун зрителей,
    режим all/trusted, атомарная запись `.clipmarks` через `core.marks.MarksFile`, t = now−ref_epoch).
  • `bot/twitch_marks_bot.py` — IRC-транспорт (socket+ssl к irc.chat.twitch.tv:6697, caps tags/
    commands, парсер `parse_irc`, реплаи «✓ метка»), Helix-онлайн (`OnlinePoller`: app-token по
    client_id/secret → viewer_count; при live ставит ноль времени = старт эфира), авто-переподключение,
    `--simulate` (тест без Twitch через stdin). Настройки — `bot/config.json` (из `config.example.json`).
  • Выбор пользователя (сессия #5): вход бота С ТОКЕНОМ (реплаи+онлайн), команды `!clip`/`!клип`/
    `!метка`, метить могут ВСЕ с кулдауном на зрителя, тест — на своём канале БЕЗ эфира (чат работает
    и офлайн). Инструкция — `bot/README.md` (токен через twitchtokengenerator.com; онлайн опц. через
    dev.twitch.tv app). `bot/.gitignore` прячет config.json/marks/*. Проверено `bot/test_marks_bot.py`
    (роли/команды/кулдаун/trusted/запись↔чтение приложением/парсер IRC — 22 ✔) + сквозной `--simulate`.
  • ✅ ПРОВЕРЕНО ВЖИВУЮ на канале пользователя GoLsik__ (бот ClipMarkBot): токен чата зашёл,
    3 метки `!clip рофл про кота` записались с заметками и разным t → приложение сделало 2 момента.
    ГРАБЛЯ (пофикшена): после connect у сокета оставался таймаут 20с (от `create_connection`),
    и на тишине в чате recv падал по таймауту → бот переподключался и терял сообщения. Фикс:
    `self.sock.settimeout(360)` после подключения (Twitch пингует ~раз в 5 мин, отвечаем PONG).
  • `bot/run_bot.bat` — запуск двойным кликом (для не-программиста): активирует venv, гонит бота,
    останов = закрыть окно. Токен пользователь вставил в `bot/config.json` (в git не попадает).
  • ⬜ Осталось: путь «по ссылке на VOD» в приложении (`link_edit`/`link_btn` — плейсхолдер) —
    автозагрузка меток от бота по URL + `yt-dlp --download-sections`. Пока источник меток = ФАЙЛ.

**ВЫКЛЮЧАТЕЛИ ПАНЕЛЕЙ — ВО ВСЕХ РЕЖИМАХ (сессия #4, доработка по фидбэку):**
- Единый контрол видимости `widgets.EyeToggle` (рисованный глаз, не эмодзи). В легенде редактора
  (`preview_panel.EditorPanel`) у КАЖДОЙ зоны — глаз справа (активен на холсте «9:16·Финалка»):
  вебка/геймплей/субтитры/ник/лого. Пишет `Placement.visible`, скрытая зона исчезает с холста.
  Работает в Этапе 1 (одиночный И мульти — редактор общий). В Этапе 2 те же 5 панелей — глаз-строки
  с цветом зоны. `main_window._build_pipeline`: `captions_enabled = comp.subtitles.visible` (глаз
  субтитров реально гасит субтитры; вебку/геймплей/ник/лого ядро уже честило).
- Легенда редактора и подписи стали **theme-aware** (`EditorPanel.set_theme`, зовётся из
  `_apply_theme`); холст-превью намеренно остаётся тёмным (как в видеоредакторах). Проверено
  `dev/shot_editor_eyes.py` (тёмн/светл, скрытая панель = перечёркнутый глаз).

**⬜ ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ / СЛЕДУЮЩЕЕ (честно, не «вайб»):**
- **Транскрипция всего файла**: `pipeline.run` распознаёт речь по ВСЕМУ источнику. Для короткого клипа
  ок, для 2–3-часового стрима — медленно. Оптимизация: транскрибировать только окна выбранных сегментов
  (пред-нарезка/диапазоны в `transcribe`). Корректность верная, вопрос скорости. НЕ трогал ядро
  транскрипции без возможности прогнать реальный рендер (ffmpeg+GPU) в этой сессии.
- **Дыры в середине**: `TimeMap` их полностью поддерживает, но UI пока даёт только «сдвиг начала».
  Редактор дыр на таймлайне (отметить пропавшие куски) — следующий шаг UI.
- **VOD по ссылке**: авто-подгрузка меток+скачивание окон (`yt-dlp --download-sections`) зависит от
  бота (кто отдаёт метки по URL) — сделать вместе с треком C.
- **Раскладка per-moment**: сейчас раскладка/звук ОБЩИЕ на клип (один стрим — одна раскладка). Переопр.
  под отдельный момент требует по-сегментного компоузинга в `render` (сейчас одна `composition` на всё).
- **Живая смена темы на экране 2**: таймлайн перекрашивается, но инлайн-стили карточек левой колонки —
  строятся под тему при запуске (обе темы корректны, если открыть в сохранённой). Полный ре-стайл
  на лету — мелкая доработка.
- ⚠️ Пользователь просил мягкие названия онлайна (НЕ «мелкий стример») и «делай параллельно что можно».

**Формат файла меток (контракт):** `{platform, streamer, broadcast_id, started_at, duration,
online, marks:[{t, type, author, note?}]}`. Канон — в `core/marks.py`.

---

## 📌 СТАТУС И ПРОДОЛЖЕНИЕ (обновлено 2026-07-19, сессия #3)

### 🎯 СЛЕДУЮЩАЯ СЕССИЯ — ЭТАП 2 (по запросу пользователя, начнёт в НОВОЙ сессии)
**Этап 2 = ВТОРОЙ РЕЖИМ бургер-меню «Метки через бота»** (`ui/mode_menu.py` MODES[1]:
«Стример метит моменты в чате»). Сейчас — ЗАГЛУШКА: `main_window._on_mode_selected(idx)`
при idx!=0 показывает QMessageBox «Скоро» и меняет только `mode_label`. Каркас режима есть,
внутрянки нет.
- **Суть Этапа 2 (из ТЗ):** СКЛЕЙКА разнесённых отрезков одного стрима в один вертикальный клип
  (несколько «моментов» → один клип). Вероятно моменты помечаются ботом в чате Twitch (по MODES).
  По РАЗВИЛКАМ спросить пользователя (он не кодит): как метятся моменты (бот в чате? ручной
  мульти-выбор отрезков на таймлайне? импорт таймкодов?), сколько отрезков, как оформлять стык.
- **ФОРВАРД-СОВМЕСТИМОСТЬ УЖЕ ЗАЛОЖЕНА (не переделывать, использовать):**
  • `config.ProjectConfig.segments: list[Segment]` — рендер уже из СПИСКА (до 4 отрезков).
  • `render.py` собирает клип concat'ом НЕСКОЛЬКИХ сегментов (проверено ядром на Этапе 1).
  • `pipeline._remap_words(words, segments)` уже переносит словные таймкоды через НЕСКОЛЬКО
    отрезков с накопленным смещением (offset). Т.е. субтитры/мат/подсветка переживут склейку.
  • ЧТО НЕ ГОТОВО: `pipeline.run()` строит `segments=[один отрезок]` (стр. ~«segments =
    [Segment(start=pcfg.start, end=end)]») — надо принимать СПИСОК; UI-таймлайн `TrimTimeline`
    (`ui/preview_panel.py`) поддерживает ОДИН отрезок (start/end) — нужен мульти-отрезковый.
- **Стиль работы (важно, см. [[user-clip-polisher]]):** перед крупной задачей — объяснить своими
  словами что понял, задать уточняющие вопросы И предложить креативные идеи, ТОЛЬКО потом делать.
  Проверять правки реально (рендеры/скриншоты через `dev/screenshot.py` + офскрин-шоты), не «на глаз».
- **Само-проверка UI:** офскрин-скрины (`dev/*.py`: screenshot/shot_*/test_*), ассистент открывает
  PNG и правит. Живой GUI кликами тестирует ПОЛЬЗОВАТЕЛЬ. Тест-клипы: `tests/sample_clips/*.mp4`.

### ⏸️ ГДЕ ОСТАНОВИЛИСЬ (конец сессии 2026-07-18 #3 — иконка/пилюля/мульти-редактор)
- **✅ РЕЛИЗ v1.0.5 ВЫПУЩЕН** (Latest): https://github.com/GoLsik-web/clip-polisher/releases/tag/v1.0.5
  (`ClipPolisher-Setup-1.0.5.exe`, 216 МБ). Версия в `core/version.py` И `installer/clip-polisher.iss`
  = 1.0.5 (синхрон). Прошлый релиз v1.0.4 удалён (release+tag). Код в git НЕ закоммичен (правки
  только в рабочем дереве) — при желании закоммитить/запушить main. У пользователя стоит v1.0.4 —
  может вживую проверить авто-апдейт 1.0.4→1.0.5. В релиз вошли фичи сессий #2 и #3.
- **Сделано и ПРОВЕРЕНО (скрины/функц.тесты/рендеры), вошло в v1.0.5:**
  0. ⏱️ **Живой прогресс мульти-рендера**: во время очереди экран НЕ перекрыт загрузчиком —
     прогресс на карточках («Идёт… %») + шапка ленты «Обработка N/M · P%», редактирование
     заблокировано (`_set_rendering_ui`), выбор клипа/добавление недоступны. Проверено скрином.
  1. 🖼️ **Иконка окна** — `MainWindow._set_window_icon()` + `app.py` ставят `assets/app.ico`
     (брендовый бургер) на окно и приложение через `resources.res` (в exe бандлится, т.к.
     spec datas=`("assets","assets")`). Была дефолтная иконка Qt/винды — теперь своя.
  2. 🔖 **Пилюля версии со статусом** (`ui/widgets.py::VersionPill`) вместо текстовой кнопки
     «Обновления». Рядом с логотипом: «v1.0.4 ●» + цветная точка (серый=проверяю,
     зелёный=актуально, бирюзовый пульсирует=обнова, янтарный=ошибка). Клик → меню обновлений.
     Theme-aware (`set_palette` из `_apply_theme`). `_style_updates_btn` теперь дергает
     `version_pill.set_status`. Проверено скринами (тёмная/светлая × актуально/обнова).
  3. 🎬 **МУЛЬТИ-РЕДАКТОР** (`ui/clip_strip.py` + `ui/main_window.py`): режим «Несколько»
     (топбар, бывш. «Пачкой»). Лента миниатюр СЛЕВА от редактора (`ClipStrip`/`ClipCard`):
     карточка = кадр+имя+длительность+статус (Ожидает/Идёт %/Готово/Ошибка)+удалить, кнопка
     «+ Добавить клипы». Модель `ClipItem` (source/duration/start/end/comp/nick/thumb/status).
     Клик по карточке → `_select_clip` грузит клип в основной редактор (кадр/зоны/обрезка/ник).
     **Шаблон + правки под клип:** зоны(composition), обрезка, НИК — индивидуальные у каждого;
     стиль субтитров/платформа/экспорт — общий шаблон из мастера. Композиция мутируется ПО
     ССЫЛКЕ (editor.set_composition(clip.comp)), обрезка/ник фиксируются `_store_active_edits`
     при переключении/рендере. `_render_batch` строит pcfg на клип (source/comp/start/end/ник+
     шаблонная платформа/стиль), файл `<имя>_vertical.mp4`, очередь через `BatchRenderThread`/
     `run_batch`. `_batch_progress` парсит индекс клипа из общего прогресса и красит статусы
     карточек. Проверено: скрин ленты+редактора (тёмн/светл), функц.тест `dev/test_multi.py`
     (по-клипно верные конфиги: ники/зоны/обрезка индив., платформа/стиль общие, разные имена),
     реальная очередь `run_batch` на 2 клипах. `_batch_sources` больше не используется.
- **ЧТО ПРОВЕРИТЬ ПОЛЬЗОВАТЕЛЮ ВЖИВУЮ:** иконку в заголовке/таскбаре; пилюлю (клик→меню,
  свечение при обнове); мульти-редактор кликами — добавить клипы, попереключать, поправить
  зоны/ник у разных клипов, «Отрендерить» очередь, статусы на карточках.
- **Dev-инструменты сессии #3:** `dev/shot_pill.py`, `dev/shot_multi.py`, `dev/test_multi.py`.
- **НА БУДУЩЕЕ (не делал):** живой прогресс по карточкам виден только ПОСЛЕ рендера (во время
  его перекрывает загрузчик-оверлей); ручной клик-выбор слов для подсветки; per-clip платформа.

## 📌 (сессия #2, 2026-07-18)

### ⏸️ ГДЕ ОСТАНОВИЛИСЬ (конец сессии 2026-07-18 #2 — 3 фичи + фикс окна обновлений)
- **Сделаны и ПРОВЕРЕНЫ (рендерами/скриншотами/тестом) 4 вещи, НО в GUI кликами их
  проверяет ПОЛЬЗОВАТЕЛЬ, и релиз НЕ выпущен (всё ещё v1.0.4):**
  1. 🎨 **Стиль окна обновлений** (`ui/updates_dialog.py`) — было: захардкоженные тёмные
     цвета, окно «чужое» на светлой теме. Стало: theme-aware через `theme.p(theme)`,
     objectName-стили, фон QDialog красится, `apply_theme()` перекрашивает на лету при
     смене темы (main_window вызывает `dlg.apply_theme`). Прокинута тема в конструктор.
     Проверено офскрин-снимком в обеих темах (`dev/shot_updates.py`).
  2. ✨ **Яркие субтитры с подсветкой ключевых слов (MrBeast)** (`core/captions.py`):
     `mark_keywords()` — ПОФРАЗОВЫЙ отбор (одно «ударное» слово на фразу: эмоция !/?,
     цифры, КАПС, длинное; служебные `_STOP` и мат `*` не трогаем; не два подряд;
     ~24% слов). Поле `Word.key`. Два стиля в .ass: `Main` + `Key`. Режимы
     `highlight_mode`: `box` (жёлтая плашка BorderStyle=3, тёмный текст) | `color`
     (яркий текст + обводка) | `none`. `apply_key_overrides()` — ручная правка {idx:bool}.
     Караоке красит ключевое инлайном. Проверено прожигом на реальном клипе (box+color+
     обычное слово) и end-to-end пайплайном (плашка «работает» + брендинг + 48кГц).
  3. 🛡️ **Safe-zone оверлеи** (`core/safezones.py` данные + `ui/preview_panel.py`):
     пресеты TikTok/Shorts/Reels — «занятые» интерфейсом зоны в долях 9:16 (правый
     столбец иконок, низ ник/подпись, верх статус). Рисуются красной штриховкой ТОЛЬКО
     в редакторе (в видео НЕ вжигаются). Чип-ряд «Safe-зоны: Выкл/TikTok/Shorts/Reels»
     в редакторе; выбор площадки уводит в 9:16-финалку и рисует зоны. Проверено скринами.
  4. 👤 **Именованные профили стримеров** (`core/profiles.py` + `ui/main_window.py`):
     хранение в `%APPDATA%\ClipPolisher\profiles.json` (Roaming, атомарная запись).
     Профиль = {nickname, platform, composition(зоны/раскладка), safezone}. Бар над
     мастером: «Профиль: [выпадашка] [Сохранить…] [Удалить]». Выбор → `_apply_profile`
     (ник/платформа/зоны/safe-зона + refresh превью). Проверено функц. тестом круга
     сохранить→применить→удалить (`dev/test_profiles.py`) — всё восстанавливается.
- **UI-проброс:** шаг «Субтитры» → чип «Подсветка ключевых слов: Выкл/Плашка/Цвет»
  (`highlight_mode` + `highlight_keywords` в PipelineConfig; работает и в batch, т.к. оба
  идут через `_build_pipeline`). Пайплайн зовёт `mark_keywords`+`apply_key_overrides`.
- **ЧТО ПРОВЕРИТЬ ПОЛЬЗОВАТЕЛЮ ВЖИВУЮ (GUI кликами, у ассистента нет дисплея):**
  окно обновлений в свете/тьме; подсветку в готовом клипе (Плашка/Цвет); safe-зоны при
  выборе площадки; сохранение/загрузку/удаление профиля стримера.
- **ПОСЛЕ проверки — РЕЛИЗ v1.0.5** по чек-листу (версия в `core/version.py` И
  `installer/clip-polisher.iss`, пересобрать exe+installer, `gh release create v1.0.5`,
  удалить прошлый). Ручной клик-выбор ключевых слов в UI пока НЕ сделан (движок
  `apply_key_overrides` готов, UI-редактора выбора слов нет — на будущее).
- **Dev-инструменты этой сессии:** `dev/shot_updates.py`, `dev/shot_editor.py`,
  `dev/test_profiles.py` (скрин/тест новых фич офскрин).

### ⏸️ ГДЕ ОСТАНОВИЛИСЬ (конец сессии 2026-07-18)
- **Опубликован публичный релиз v1.0.4:** https://github.com/GoLsik-web/clip-polisher
  (Latest, `ClipPolisher-Setup-1.0.4.exe`, 205 МБ). gh авторизован как GoLsik-web.
- **Версия сейчас: 1.0.4** (`core/version.py` + `installer/clip-polisher.iss` в синхроне).
- **На машине пользователя стоит v1.0.3** — специально, чтобы он вживую проверил
  авто-обновление до v1.0.4 (кнопка «Обновления» загорится → «Обновить»). Это его тест.
- **Всё в этой сессии сделано и проверено (детали ниже):** битое видео (loudnorm 96→48к),
  плавный прогресс, выбор папки/имени сохранения, реалистичный бургер → зацикленная анимка,
  таймлайн-обрезка, batch-режим, сборка .exe + установщик, докачка модели+CUDA при 1-м запуске
  (проверено реальной докачкой из exe), настоящие лого Twitch/YouTube/Kick, встроенное
  обновление + меню обновлений с «Что нового».
- **НЕ проверено вживую (для будущего):** транскрипция из установленного exe на СКАЧАННЫХ
  CUDA-DLL (venv-тест шёл на своих CUDA; сама докачка+распаковка в exe прошла). Полный клик-тест
  меню обновлений в живом GUI (логика/детект/скачивание/установка-поверх проверены по частям).
- **СЛЕДУЮЩЕЕ (по запросу пользователя, отложено):** safe-zone оверлеи под TikTok/Shorts;
  яркие субтитры с выделением ключевых слов; профиль стримера (зоны/ник/платформа под стримера,
  галочка «Запомнить», хранение в %APPDATA%). Хук-заголовок сверху НЕ выбран.
- **РЕЛИЗ-ЧЕК-ЛИСТ** (обязательно синхронно!): версия в `core/version.py` И
  `installer/clip-polisher.iss`; пересобрать (`python -m PyInstaller --noconfirm --clean
  clip-polisher.spec` → ISCC `installer/clip-polisher.iss`); `gh release create vX.Y.Z <setup>
  --latest`; удалить прошлый релиз. Тег = версии (иначе апдейтер не сработает).

---

**СЕССИЯ 2026-07-18 — фиксы багов + 2 фичи (всё проверено):**
- 🐞 **БИТОЕ ВИДЕО ИСПРАВЛЕНО** (клип «играл 2 сек и виснул / не открывался»). Причина —
  фильтр `loudnorm` РЕСЕМПЛИТ звук до 192 кГц, `aresample=48000` стоял ДО него → AAC на
  выходе 96 кГц (плееры/соцсети давятся). Фикс: `aresample=48000` ПОСЛЕ loudnorm
  (`core/audio.py`). Плюс: конечный тон бипа вместо бесконечного `sine`+`-shortest`
  (обрезал видео из-за задержки loudnorm), `-fps_mode cfr`, и `_validate_output()` в
  `render.py` (ffprobe после рендера: нет файла/потока/частота>48к/обрезка → падаем громко).
  Проверено полным прогоном: 48 кГц, декод 0 ошибок, 30.0 c.
- 🐞 **«КОНФЛИКТ С ДРУГОЙ ПРОГОЙ» — миф.** Скачущие проценты — наш баг прогресса, не
  соседнее приложение. На всякий случай temp-файлы (`clip_polisher_clean_<pid>.wav`,
  nvenc-тест) сделаны уникальными по PID (два экземпляра не затрут друг друга).
- 📊 **ПРОГРЕСС ПЛАВНЫЙ И МОНОТОННЫЙ**: `ui/loader.py` — `_target`/`_display` + QTimer 60fps,
  назад НИКОГДА (убит откат 70→50→72), лёгкий дополз при простое, `finish()`→100%. Whisper
  теперь шлёт реальный прогресс (`transcribe_file(on_progress=)` по `seg.end/duration`),
  этап распознавания заполняет 10→52%. Проверено: откат цели игнорируется.
- 💾 **СОХРАНЕНИЕ**: шаг «Экспорт» — папка (кнопка «Изменить…», помнится в QSettings
  `out_dir`) + имя файла + «Сохранить как…». `unique_path()` (`config.py`) → не
  перезаписывает, кладёт «имя (2).mp4». Промежуточные файлы (субтитры, превью-кадры,
  киноленты) уведены в temp (`clip_polisher_work`) — рядом с итогом только он.
- 🍔 **БУРГЕР В ЗАГРУЗЧИКЕ — реалистичный** (булка с кунжутом/салат/помидор/сыр с подтёками/
  котлета), собирается КАСКАДОМ с падением-отскоком-сплющиванием (`ui/burger.py`:
  `REAL_LAYERS`, `_draw_ingredient`, новый `BurgerLoader`). Иконка топбара и морф меню —
  НЕ тронуты (там прежний палитровый `draw_burger`).
- ✂️ **ТАЙМЛАЙН ОБРЕЗКИ — интерактивный** (`preview_panel.TrimTimeline`): кинолента
  стоп-кадров (`ffmpeg_utils.make_filmstrip`, генерится потоком `worker.FilmstripThread`) +
  2 ручки начала/конца + тяга отрезка целиком. Двусторонний синхрон с числовыми полями
  шага 3 (через `_syncing`-флаг, без рекурсии). Проверено.
- 📦 **BATCH-РЕЖИМ подключён** (`main_window._render_batch`): кнопка «Пачкой» → «Файл…»
  мультивыбор → список; рендер каждого клипа с ТЕКУЩИМИ настройками в свой файл
  `<имя>_vertical.mp4` через `BatchRenderThread`/`pipeline.run_batch`. (Живьём в UI кликами
  не гонял — проверил конструкцию pcfg и импорт; движок run_batch проверен ядром.)

**СБОРКА И РАЗДАЧА (сделано в эту же сессию):**
- 📦 **Публичный репозиторий: https://github.com/GoLsik-web/clip-polisher**, релиз v1.0.0 с
  установщиком `ClipPolisher-Setup-1.0.0.exe` (202 МБ). gh авторизован как GoLsik-web.
- 🏗️ **PyInstaller** (`clip-polisher.spec`, onedir) → `dist\ClipPolisher\` (~791 МБ). НЕ включает
  CUDA (исключён `nvidia`) и модель — иначе не влезает в лимит GitHub 2 ГБ. Исключены тяжёлые
  неиспользуемые Qt-модули (QtWebEngine и пр.: PySide6 634→92 МБ). ffmpeg+ffprobe (полный
  Gyan-билд, 462 МБ) кладутся в `_internal\ffmpeg\`; `ffmpeg_utils._bundled()` берёт их оттуда.
  Пересборка: `python -m PyInstaller --noconfirm --clean clip-polisher.spec`.
- 🧰 **Установщик — Inno Setup** (`installer\clip-polisher.iss`, ISCC в
  `%LOCALAPPDATA%\Programs\Inno Setup 6\`). Ставит БЕЗ прав админа в
  `%LOCALAPPDATA%\Programs\ClipPolisher`. Компиляция → `installer\Output\`. Цепочка
  установка→запуск→удаление проверена (тихий режим). Размер сжатого установщика ~202 МБ.
- 🌐 **Первый запуск (`core/provision.py`)**: качает модель large-v3 с HF (`Systran/
  faster-whisper-large-v3`) + CUDA-колёса с PyPI (ЗАПИНЕНЫ под ctranslate2 4.8.1: cublas
  12.9.2.10, cudnn 9.24.0.43, nvrtc 12.9.86; cuda-runtime НЕ нужен) в `%LOCALAPPDATA%\
  ClipPolisher\{models,cuda}`. `transcribe.py` грузит модель оттуда (offline) и добавляет
  CUDA-DLL в путь через `provision.add_cuda_to_path()`. UI: экран докачки при старте
  (`_check_first_run`, поток `ProvisionThread`); пропуск — env `CLIP_SKIP_PROVISION=1`.
  Ресурсы (шрифты/иконки) — через `core/resources.res()` (dev-корень / sys._MEIPASS).
- ⚠️ **НЕ проверено вживую (нужен реальный первый запуск на 5 ГБ):** сам процесс докачки
  модели+CUDA и транскрипция ИЗ установленного exe. Проверено: exe стартует (окно, 107 МБ),
  URL-колёс резолвятся, пути ресурсов, установка/удаление. Стоит прогнать реальный первый
  запуск на чистой машине/после очистки `%LOCALAPPDATA%\ClipPolisher`.

**ВСТРОЕННОЕ ОБНОВЛЕНИЕ (v1.0.3):** `core/version.py` (`__version__`) — единый источник
версии. `core/updater.py` — проверка последнего GitHub-релиза (`releases/latest`), сравнение
версий, скачивание установщика (через `provision._download`), `launch_installer`. UI: тихая
проверка при старте (`_check_update`), кнопка «Обновить до vX» в топбаре, скачивание с
прогрессом → запуск установщика → `QApplication.quit()`. Установка ПОВЕРХ работает за счёт
одного AppId + `CloseApplications=yes` в .iss (проверено: 1.0.2→1.0.3, одна запись).
**РЕЛИЗНЫЙ ЧЕК-ЛИСТ (держать в синхроне!):** поднять версию в `core/version.py` И
`installer/clip-polisher.iss` (#define MyAppVersion), пересобрать exe+installer, `gh release
create vX.Y.Z <setup> --latest`, удалить прошлый релиз. Апдейтер сравнивает `__version__` с
тегом релиза, поэтому они ДОЛЖНЫ совпадать.

**ЧТО ДАЛЬШЕ (по запросу пользователя, ПОСЛЕ фиксов — фичи):**
- Safe-zone оверлеи под TikTok/Shorts (где UI площадки перекрывает кадр — чтобы субы/ник туда
  не попадали). Яркие субтитры с выделением ключевых слов (MrBeast-стиль). Профиль стримера
  (зоны/ник/платформа под конкретного стримера, галочка «Запомнить», хранение ЛОКАЛЬНО у
  пользователя — `%APPDATA%`). Сборка `.exe` (PyInstaller). Хук-заголовок сверху — НЕ выбран.

---

## 📌 (прежний статус 2026-07-16)

**Ядро (core/) — ГОТОВО и проверено на реальных Twitch-клипах:** ingest, transcribe
(денойз+улучшенные параметры), profanity, captions (.ass, 4 анимации), layout/compositor
(свободная композиция + пресеты A/B/C/D), branding, audio (loudnorm + бип/тишина), render
(NVENC/libx264, N сегментов), preview, pipeline. Полный прогон клип→вертикаль работает.

**UI (ui/) — РАБОТАЕТ, отполирован:** полноэкранная раскладка 35/65 (фикс, без дивайдера),
бургер-меню режимов с морфом, редактор с переключателем 16:9⇄9:16, свободная композиция на 9:16
(drag/resize/rotate, вебка поверх), эксклюзивный выбор зон через кнопки-легенду, загрузчик-бургер,
тёмная/светлая тема, анимации. Краш при смене формата ИСПРАВЛЕН (жизненный цикл QThread).

**Как проверять UI:** `dev/screenshot.py` → PNG в `out/shots/` (offscreen, grab). Ассистент
ОТКРЫВАЕТ png и правит визуал сам. Запуск приложения: `run.bat`. Окружение готово
(FFmpeg+h264_nvenc, faster-whisper GPU). Тест-клипы в `tests/sample_clips/` (gitignored).

**ЧТО ДАЛЬШЕ (не сделано):**
- Интерактивный ТАЙМЛАЙН обрезки со стоп-кадрами (сейчас заглушка + числовые поля start/end).
- BATCH-режим в UI (backend `pipeline.run_batch` готов; кнопки «По одному/Пачкой» пока без логики).
- Сборка в `.exe` (PyInstaller): ffmpeg/шрифты/иконки/модель через sys._MEIPASS.
- Возможные доработки по фидбэку: агрессивность денойза/переключатель «улучшать звук»;
  тайминги/плавность морфа; светлая тема — вторичный текст бледноват (theme-aware).
- Проверить критерии готовности Этапа 1 (см. docs/SPEC-stage1.md).

---

Короткая память проекта, чтобы не переоткрывать контекст каждую сессию.
Полное ТЗ: **`docs/SPEC-stage1.md`** (сверяйся с ним, не проси пользователя вставлять заново).
**UX-эталон интерфейса: `docs/ui-reference.html`** (копия `klip-polirovshik-prototip.html`) —
ОТКРЫВАТЬ и сверяться при вёрстке UI (компоновка, флоу, цвета, анимации).

## Что за проект

Десктоп-приложение под Windows: горизонтальный клип стрима (файл ИЛИ ссылка на
Twitch-клип) → вертикальный клип 9:16 (TikTok/Shorts), готовый к постингу:
раскладка (вебка + геймплей), анимированные субтитры, брендинг, нормализованный звук.

**Текущий этап: Этап 1** из трёх. (Этап 2 — склейка разнесённых отрезков; Этап 3 —
авто-поиск моментов ИИ. Сейчас их НЕ делаем.)

## Стек (зафиксирован, не менять без спроса)

- Python 3.11+ (на машине 3.12.10)
- FFmpeg через subprocess, GPU-кодек **h264_nvenc** (фолбэк libx264)
- faster-whisper, модель **large-v3**, на GPU (CUDA), `word_timestamps=True`
- Субтитры: генерация **.ass** (libass) — стили/обводка/фон/анимации
- yt-dlp — скачивание клипов по ссылке
- PySide6 (Qt) — интерфейс
- PyInstaller → .exe (без цифровой подписи)

## Железо

Windows 11, **RTX 3060 Ti (8 ГБ VRAM)**, драйвер 610.74. Обработка локально на GPU.
VRAM 8 ГБ: `compute_type="float16"` (или `int8_float16`); при OOM — фолбэк на `medium`.

## Главный архитектурный принцип

Жёсткое разделение слоёв:
- **`core/`** — вся логика, БЕЗ Qt. Чистые функции/классы, вызов из CLI/бэкенда.
- **`ui/`** — тонкая оболочка PySide6: собирает настройки, вызывает `core`.
- **`core` НИКОГДА не импортирует `ui`.**

Любую фичу: сначала `core` + тест на реальном файле через CLI, потом UI.

## Forward-compat (заложено под Этап 2)

Рендер собирает клип из **СПИСКА сегментов** `[(start, end), ...]`, а не из одного
отрезка. На Этапе 1 список всегда из одного элемента. Модель данных и код рендера
без переделки держат до 4 сегментов с настраиваемым стыком. UI склейки сейчас НЕ делаем.

## Принятые дефолты

- Разрешение экспорта: **1080×1920**, опция 720×1280. FPS: **30**, опция 60.
- Аудио: loudnorm EBU R128 **I=-14, TP=-1.5**; бип-тон **1 кГц** поверх мата.
- **Мат-фильтр по умолчанию ВЫКЛЮЧЕН** (переключатель в UI). [обновлено по новому ТЗ]
- Раскладка по умолчанию: **пресет A** (вебка сверху + геймплей снизу). Зоны — в долях кадра.
- Субтитры: 4 анимации (Pop / Fade / Slide-up / Karaoke), реестр расширяемый.
- Брендинг: ник + иконка платформы (Twitch/YouTube/Kick/нет), дефолт — уголок.
- Whisper: large-v3, float16, GPU; лог устройства обязателен.

## Типичные грабли (детали — в SPEC, раздел «ТИПИЧНЫЕ ГРАБЛИ»)

- Whisper GPU: тихий откат на CPU / «cublas/cudnn not found» → ЯВНО логировать устройство.
- h264_nvenc: есть не во всех сборках FFmpeg + завязан на версию драйвера → проверять
  реальным тест-энкодом, фолбэк libx264 с предупреждением.
- .ass: цвет `&HAABBGGRR` (BGR + инверт. альфа), тайминги `H:MM:SS.cs`.
- Кириллица в libass: класть шрифт с кириллицей в assets/fonts, явно указывать fontsdir.
- NVENC: только ЧЁТНЫЕ ширина/высота — все кропы/паддинги приводить к чётным.
- A/V-синхрон после фильтров; пути с пробелами/кириллицей; парсинг `-progress` для прогресс-бара.
- **loudnorm РЕСЕМПЛИТ до 192 кГц** → без `aresample=48000` ПОСЛЕ него AAC выходит 96 кГц,
  и файл «играет 2 сек и виснет»/не открывается в плеерах и соцсетях. Всегда возвращать 48 кГц.
- `-shortest` + бесконечный `sine` (бип): из-за задержки loudnorm режет видео раньше времени.
  Тон делать КОНЕЧНЫМ (`sine=...:duration=<клип+1>`), amix=first, без глобального `-shortest`.
- `-progress`/`out_time_ms` НЕ монотонен при видео+аудио (буфер loudnorm убегает) → прогресс-бар
  в UI держать монотонным (`_target/_display`, назад не откатывать).

## Рабочий процесс

Инкрементально, маленькими проверяемыми кусками. Каждый модуль: сначала `core` +
тест на реальном файле, показать результат, потом UI. По важным развилкам —
спрашивать пользователя (он не программист). Мелочи — разумный дефолт + запись в README.

## Статус окружения

Всё готово: FFmpeg 8.1.2 + h264_nvenc работают (драйвер обновлён до 610.74),
faster-whisper видит GPU (CUDA, float16). Проверено полным рендером 1080×1920.

## СВОБОДНАЯ КОМПОЗИЦИЯ (новая модель раскладки, по запросу пользователя)

- `config.Placement` (x,y,w,h в долях 9:16 + rotation° + shape rect/circle + visible) и
  `config.Composition` (webcam_source/gameplay_source = ЧТО вырезать из 16:9;
  webcam/gameplay/subtitles/nick/platform = КУДА на 9:16). `composition_from_preset(A/B/C/D)`.
- `compositor.build_composition_segment` — FFmpeg-граф: чёрная база → геймплей → вебка
  (crop источника → cover-scale в rect → circle-маска → rotate → overlay; вебка последней=поверх).
  Проверено рендером: A/B/C + поворот 12° — корректно.
- `render.py`: если `cfg.composition` задана — путь композиции; брендинг по прямоугольникам
  (`branding.build_branding_at`); субтитры — позиция из composition.subtitles (pipeline).
- `audio.build_audio_filter(mode=)`: 'beep' (тон 1 кГц) | 'silence' (заглушить, без тона).
- `preview.freeze_preview(composition=)` — 9:16 результат по композиции (для превью в UI).
- `pipeline`: PipelineConfig.composition + profanity_mode ('beep'|'silence'); profanity_enabled=off→не трогать.

## UI ПЕРЕСТРОЕН под свободную композицию (проверено скриншотами)

- Топбар: **гамбургер** (`widgets.HamburgerButton`) → `ui/mode_menu.ModeMenuOverlay`
  (затемнение фона + карточки режимов, fade-in). Режим показан в топбаре текстом.
- Компоновка **35/65**: QSplitter [мастер настроек | РЕДАКТОР], resizable, <1120px → вертикаль.
- `ui/preview_panel.EditorPanel` — редактор с переключателем формата **16:9·Источник ⇄ 9:16·Финалка**:
  на 16:9 — зоны источника (что вырезать); на 9:16 — ВСЕ 5 элементов свободно
  (drag/resize/**rotate** через ZoneBox с ручкой поворота), **вебка всегда поверх** (raise_).
  Пресеты A/B/C/D пересобирают композицию. Легенда+таймлайн под холстом.
- Шаг «Субтитры»: **чипы цензуры** «Не трогать / Бип / Заглушить» вместо тумблера (дефолт — не трогать).
- Старый rail/PreviewPanel удалён; dev/screenshot.py снимает и source, и final режимы + меню режимов.

## UI-полировка (итерация «на весь экран + бургер»)

- Раскладка НА ВЕСЬ ЭКРАН (убрано центрирование/max-width). Мастер|Редактор — ФИКС 35/65
  через QHBoxLayout со stretch (БЕЗ дивайдера-сплиттера); <1120px → вертикаль (setDirection).
- `ui/burger.py`: `draw_burger` (слои снизу-вверх: булка, салат, котлета, сыр, помидор, соус,
  верхняя булка+кунжут), `BurgerButton` (иконка в топбаре), `BurgerLoader` (сборка по слоям
  по прогрессу + падение верхнего слоя). Загрузчик = собирается бургер.
- `mode_menu`: МОРФ бургера — по клику слои бургера «разъезжаются» из иконки (интерполяция
  прямоугольников слоёв от стопки-бургера у якоря к полоскам меню, Property `morph` 0→1),
  затем проявляется контент карточки (opacity во 2-й половине), фон затемняется (рисуем сами
  в paintEvent через `dim`, НЕ graphics-effect — иначе конфликт painter при grab). Слева-сверху.
- Бургер ПОД ПАЛИТРУ (не еда): белые слои (`FILL`) + фиолетовый акцент (`ACCENT`), котлета/кунжут
  акцентом. `draw_burger` универсален (иконка/загрузчик/морф). Проверено скриншотами стадий.
- `background`: добавлены мягкие угловые свечения (наполненность), сфера гуляет по четвертям.
- ГРАБЛЯ Qt: QWidget не красит stylesheet-фон без `WA_StyledBackground` (чинил затемнение
  загрузчика). Символы вне PT Sans (☰●◑→✨🎬✓) → tofu, заменять на рисованное/текст.
- Само-проверка: dev/screenshot.py снимает шаги/финалку/светлую/меню/загрузчик; ассистент
  открывает PNG и правит до чистоты. Проверено 1920/1280/1000/820.

## Итерация «выбор зон + фиксы багов»

- КРАШ ИСПРАВЛЕН: смена формата после загрузки клипа рушила приложение — причина
  `QThread: Destroyed while thread is still running` (потоки хранились в одном атрибуте и
  перезаписывались на ходу). Фикс: `_track()` держит ссылки в `self._threads` (удаляем по
  `finished`), превью с ДЕБАУНСОМ (`_preview_running/_preview_pending` — не плодим потоки),
  `closeEvent` ждёт потоки (`th.wait`). Проверено: 5 быстрых refresh → 1 поток; смена формата ок.
- Легенда → КНОПКИ ВЫБОРА зоны (эксклюзивно): чтобы редактировать зону, надо сначала нажать
  её кнопку; редактируется ТОЛЬКО выбранная (`ZoneBox.editable`, у остальных
  `WA_TransparentForMouseEvents` — клики проходят мимо, пунктирная приглушённая рамка, без ручек).
  Выбранная зона поднимается наверх (raise). Кнопки недоступны для зон, которых нет в текущем холсте.
- Холст ВПИСЫВАЕТСЯ (contain) в доступное место (`CanvasArea.refit`), а не растёт огромным в 9:16 —
  легенда-кнопки и таймлайн всегда видны снизу. Редактор заполняет высоту колонки.
- Бургер УПРОЩЁН до 5 слоёв (2 булки белые + 3 начинки = 3 режима, цвета окон STEP_COLORS[0..2]);
  морф: начинки → 3 кнопки меню (цвета совпадают), полоски РАСТВОРЯЮТСЯ (alpha-fade) по мере
  проявления контента — без грязного наложения.
- Полный сценарий вход→редактирование→смена формата→рендер прогнан на реальном клипе — без краша,
  рендер из UI-конфига даёт mp4.

## Качество распознавания субтитров (улучшено)

- «СЛЫШИТ ЛУЧШЕ»: перед распознаванием — очистка речи FFmpeg-фильтрами
  (`_enhance_speech`): highpass 95 + `afftdn` (FFT-денойз фоновой музыки/шума) + lowpass 7800 +
  `dynaudnorm` (динамич. нормализация — поднимает тихий голос), моно 16 кГц. `denoise=True` по умолч.
  Видимый эффект: «скажу кушай» → «Слушай»; ловит мат/имена, которые терялись.
- «ПОНИМАЕТ ЛУЧШЕ»: beam_size=5, best_of=5, patience=2, temperature-фолбэк,
  condition_on_previous_text=True, no_repeat_ngram_size=3 + repetition_penalty (меньше повторов),
  log_prob_threshold=-1.2 (не выбрасываем неуверенные), hallucination_silence_threshold=2.0
  (убирает «странные» слова в тишине). VAD мягче (threshold 0.5→0.35, speech_pad 250) — ловит тихое.
- ТАЙМИНГИ: словные тайминги санитайзятся (end>start); в captions `_word_end` держит слово
  непрерывно до следующего, но в долгой паузе (>0.7с) прячет после короткого удержания
  (не зависает в тишине). Параметры transcribe вынесены (initial_prompt/denoise/vad).

## Прогресс по модулям

- ✅ `config.py` — модель данных (Segment/Zone/LayoutConfig/ExportConfig/ProjectConfig, JSON).
- ✅ `ffmpeg_utils.py` — ffprobe, реальная проверка NVENC, запуск с парсингом прогресса.
- ✅ `layout.py` — 4 пресета (A/D/C реализованы, B с круглым PiP); проверен A.
- ✅ `render.py` — конвейер из СПИСКА сегментов, concat, NVENC→libx264 фолбэк, CLI.
- ✅ `ingest.py` — yt-dlp (Twitch-клипы) + локальные файлы; проверен на 2 реальных клипах.
- ✅ `transcribe.py` — faster-whisper large-v3, GPU (cuda/float16), словные таймкоды,
  фолбэки (int8_float16 → medium → cpu); проверен на реальном клипе (63 слова, ru).
- ✅ `profanity.py` — русский мат по корням (словоформы) + EXACT/WHITELIST, маскировка
  (х**) + интервалы для бипа; проверен на транскрипте (нашёл «дрочи»→«д****»).
- ✅ `captions.py` — .ass (libass): стиль/обводка/плашка, 4 анимации (pop/fade/slide_up/
  karaoke), реестр ANIMATIONS. Кириллица подтверждена. ВАЖНО: в `Format:` событий
  ОБЯЗАТЕЛЬНО поле `Name` (Layer,Start,End,Style,**Name**,MarginL,MarginR,MarginV,Effect,Text),
  иначе Text захватывает лишнюю запятую — грабля .ass, ловили её.
- ✅ `audio.py` — loudnorm EBU R128 (I=-14,TP=-1.5) + бип 1 кГц поверх мата. Бип
  подтверждён FFT (ровно 1000 Гц на интервале). ВАЖНО: amix=duration=first + -shortest,
  иначе бесконечный sine-вход рендерится вечно.
- ✅ `branding.py` — ник (drawtext PT Sans) + иконка платформы через movie=; углы. Проверен.
- ✅ `preview.py` — стоп-кадр (раскладка+брендинг+субтитры, output-seek) или картинка. Проверен.
- ✅ `render.py` — ИНТЕГРАТОР: раскладка→concat→брендинг→субтитры→[видео];
  concat→бипы→loudnorm→[аудио]; за один проход, NVENC/libx264. RenderExtras.
- ✅ `pipeline.py` — оркестратор ingest→transcribe→remap→profanity→captions→render;
  run() и run_batch() (пачкой). Проверен полным прогоном на реальном клипе.
- ✅ Валидация: пресеты A/B/C/D и анимации pop/fade/slide_up/karaoke + плашка —
  проверены на реальных клипах (клип 1 Minecraft — A/B/D; клип 2 вождение — C).
- ✅ `presets/layout_{A,B,C,D}.json` + `layout.load_preset()`; pipeline CLI берёт зоны оттуда.
- 🟡 UI (PySide6) по UX-эталону — ПЕРВАЯ ВЕРСИЯ СОБРАНА, проходит smoke-тест, поднимается
  на реальном Windows-плагине без ошибок. Файлы:
  - `ui/theme.py` — палитра (dark/light) + QSS + step_qss (цвет окна).
  - `ui/background.py` — фон: точечная сетка + летающая перекрашивающаяся сфера (time-based, кэш-пиксмап).
  - `ui/widgets.py` — HelpIcon «i», ChipRow, ToggleSwitch (все с set_accent).
  - `ui/wizard.py` — аккордеон-мастер (WizardStep+Wizard), анимация maxHeight+opacity, цвета окон.
  - `ui/preview_panel.py` — два экрана (16:9 интерактивные зоны источника + 9:16 результат),
    легенда, таймлайн. ZoneBox: перетаскивание + масштаб углом + snapping.
  - `ui/loader.py` — оверлей-хлопушка + шутливые статусы + реальный прогресс.
  - `ui/main_window.py` — сборка: топбар (По одному/Пачкой, тема) + рейка режимов + мастер +
    превью + фон + загрузчик; связь с core через ui/worker.py (QThread).
  - `app.py` — HiDPI PassThrough; запуск через `run.bat`.
  ⚠️ Живой GUI ассистент кликами не тестирует (нет дисплея) — визуал/плавность/интерактив
  проверяет ПОЛЬЗОВАТЕЛЬ и даёт фидбэк. `ui/zone_editor.py` (старый) больше не используется.
- ✅ САМО-ПРОВЕРКА СКРИНШОТАМИ: `dev/screenshot.py` рендерит окно в PNG (offscreen,
  QWidget.grab) на 4 размерах + светлая тема → `out/shots/`. Ассистент ОТКРЫВАЕТ PNG и
  визуально проверяет вёрстку сам. ВАЖНО: offscreen без системных шрифтов → в скрипте
  грузим PT Sans, иначе текст «квадратами». Символы вне PT Sans (●◑→⬇✓) в UI заменены на
  безопасные (цветная точка-QLabel, «Тема», текст без стрелок), иначе tofu.
- ✅ UI ОТПОЛИРОВАН по скриншот-циклу: все 6 шагов видны стопкой (был баг — opacity-эффект
  вешался на весь шаг, прятал заголовки свёрнутых; теперь эффект только на body). Аккордеон:
  раскрытие OutBack(overshoot 1.05), сворачивание OutCubic, после раскрытия maxHeight снимается
  (карточка ровно по контенту). Разноцветность окон 1–6 работает (шаг 4 зелёный и т.д.).
  Адаптив: ≥1120px — 3 колонки горизонтально; <1120 — QSplitter переключается в вертикаль
  (рейка→мастер→превью стопкой). Проверено на 1920/1280/1000/820 + светлая.
  Известные точки доработки: (1) в вертикальном режиме мастер коротковат (сплиттер делит
  высоту с высоким превью; шаги скроллятся) — лучше бы внешний вертикальный скролл;
  (2) светлая тема: вторичный «приглушённый» текст в inline-стилях захардкожен под тёмную,
  на светлом бледноват — сделать theme-aware.
- ✅ ВЁРСТКА ПЕРЕДЕЛАНА на адаптив (была сломана из-за фикс-пикселей из HTML):
  3 колонки в QSplitter (рейка по контенту / мастер min-ширина / превью stretch=1),
  никаких фикс-ширин, контент центрирован (max 1500) с гуттерами под сферу, прозрачный
  QScrollArea при нехватке места, min-size окна 880×580. Превью — DualPreview: два экрана
  рядом с сохранением 16:9 и 9:16 (геометрия в resizeEvent/heightForWidth), зоны — дети
  рамки по долям. Сфера крупнее/ярче. Раскрытие шагов — пружинистое (OutBack). Проверено
  smoke-тестом на 6 размерах окна: аспекты верные, зоны внутри рамки, ошибок нет.
- ⬜ Дальше по фидбэку: полировка визуала/анимаций; интерактивный таймлайн-обрезка со
  стоп-кадрами; зоны субтитров/ника/платформы на 9:16; batch-режим UI; PyInstaller → .exe.

## Палитра/цвета из эталона (docs/ui-reference.html)

- Тёмная: bg #08080d, panel #111019, panel2 #191826, line #2a2740, text #f9f8ff,
  muted #c2bde0, accent #7c5cff. Светлая: bg #edeaf6, panel #fff, text #0d0b14, accent #6a3cff.
- Зоны (постоянные): вебка #ff5aa0, геймплей #5590ff, субтитры #f4b13f, ник #57d081, платформа #ff8149.
- Окна 1–6: #7c5cff, #5590ff, #37c9c2, #57d081, #f0a93a, #ff6ba6.

## Зоны реальных клипов (streamer eg0rl1ke, обе камеры снизу-слева)

- Игровой клип (1920×1080, чёрные полосы): вебка ≈ Zone(0, 0.73, 0.235, 0.265),
  геймплей ≈ Zone(0, 0.13, 1.0, 0.74). Эти значения в presets/layout_A/B/D.json.
- Клип 2 (вождение): вебка ≈ Zone(0, 0.66, 0.30, 0.335) — в presets/layout_C.json.
- В UI пользователь рисует зоны мышью под конкретный клип; JSON — стартовые значения.

## Весь core ГОТОВ и проверен на реальном Twitch-клипе (клип 1, Minecraft eg0rl1ke)

Полный прогон `python -m core.pipeline <клип> --preset A --anim pop --nick eg0rl1ke
--platform twitch` даёт готовый вертикальный 1080×1920 mp4: раскладка + анимированные
субтитры (кириллица) + брендинг + мат-фильтр (текст «д****» + бип 1 кГц) + loudnorm, на GPU.

Примечание по зонам: дефолтные зоны пресета A не совпадают с конкретным клипом
(вебка у eg0rl1ke снизу-слева, а дефолт — сверху-слева). Зоны рисуются под клип —
это задача UI (разметка мышью). Механизм раскладки корректен.

## Шрифты

- `assets/fonts/PTSans-Bold.ttf` + `PTSans-Regular.ttf` (ParaType, OFL, кириллица).
  Внутреннее имя семейства — **"PT Sans"** (проверено, libass находит по нему).
  В ffmpeg-фильтре субтитров: `ass=<path>:fontsdir=assets/fonts`.

## Тестовые клипы (в tests/sample_clips, gitignored)

- `3100328498.mp4` — 1920×1080@60, 30 c, канал eg0rl1ke. **Есть мат** (для мат-фильтра).
- `3173737928.mp4` — 1920×1080@60, 60 c, канал eg0rl1ke.
